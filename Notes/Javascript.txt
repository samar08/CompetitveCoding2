
convert string to float
var float_val=parseFloat(string_val)


slice an array in javascript
var arr=['a','b','c','d','e','f'];
arr.slice(0,3) returns 0,1,2

how to sort an array in javascript
const points = [40, 100, 1, 5, 25, 10];
// ascending
points.sort(function(a, b){return a - b});
//descending
points.sort(function(a, b){return b - a});

//check if a string is empty 
if(!string_variable){

}

//to convert a string to lowercase
String_variable.toLowerCase();

to trim 
String_variable.trim();

//length of array
arr.length;

//set in javascript
let myset = new Set();

// Append new elements to the 
// set using add() method
myset.add(2);
myset.add(20);

// As 20 exists, it will return true
console.log(myset.has(20));

How to check if a string has a substring in javascript
const string = "foo";
const substring = "oo";

console.log(string.includes(substring)); // true

Maximum number in javascript
let x = Number.MAX_VALUE;
Number.MAX_VALUE returns the largest number possible in JavaScript.

Number.MAX_VALUE has the value of 1.7976931348623157e+308.


How to iterate through hashmap in javascript
var map=new Map();
map.forEach(()=>())